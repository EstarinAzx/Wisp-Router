{
  "name": "wisp-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "bun run --cwd packages/vscode compile",
    "test": "bun run --cwd packages/core test"
  }
}
